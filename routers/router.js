const express = require('express');
const router = express.Router();
const {
    
} = require("../controllers/");

router.get("/bike/map/storage", )    // 자전거보관소
router.get("/bike/map/rental", )     // 자전거대여소
router.get("/bike/map/repair", )     // 자전거수리소
router.get("/bike/map/airpump", )    // 자전거공기주입기
router.get("/bike/map/lodge", )      // 자전거숙박시설

